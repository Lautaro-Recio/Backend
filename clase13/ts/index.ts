/* const lista:Array<number> = [2,3,4,45]

lista
    .map((x:number) :number => x*x);
lista.forEach((x:number) :void => console.log(x))

const generateRandom = (():number => Math.round(Math.random()* 255)) */